IS_DEV=true
PORT=3000
BOT_TOKEN=
JWT_SECRET=
# Параметры базы данных (Единый источник истины)
DB_USER=myuser
DB_PASSWORD=mypassword
DB_NAME=mydb
DB_PORT=5432
DB_HOST=localhost

# URL для Prisma (собирается из переменных выше)
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"


FRONTEND_URL = https://prices-drilling-peace-festival.trycloudflare.com
